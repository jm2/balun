# Exact direct dependencies for Balun's JSON invocation of the vendored
# flatpak-cargo-generator. The helper verifies them before execution. This is
# also a constraint input to generator-lock-requirements.txt, which pins the
# complete CI/release dependency closure and its reviewed wheel hashes.
# PyYAML is optional upstream and omitted because Balun never requests YAML.
aiohttp==3.14.3
tomlkit==0.15.1
