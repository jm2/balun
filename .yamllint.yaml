# YAML lint policy shared by CI and `yamllint --strict .`.
extends: default
rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  line-length:
    max: 120
  truthy:
    check-keys: false
